# Contributing

- Linting is executed from the repository root via `npm run lint`, which proxies to `slidev_frontend` scripts.
- If running in CI, ensure working directory is the repo root (this folder).
- Slide deck code and configuration live under `slidev_frontend/`.
