{
  "name": "hms-presentation-root",
  "private": true,
  "scripts": {
    "lint": "cd slidev_frontend && npm run lint || true",
    "lint:fix": "cd slidev_frontend && npm run lint:fix || true"
  }
}
